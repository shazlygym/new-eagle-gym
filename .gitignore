node_modules/
dist/
dev-dist/
*.local
.DS_Store

# The Android signing key and its passwords. Committing them would let anyone
# with repo access publish an update as this app — and the history would keep
# them long after the file was deleted. They belong offline, not here.
*.keystore
*.jks
signing-key-info.txt
*.apk
*.aab
